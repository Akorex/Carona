import Notifications from "../models/notifications";
import { Request, Response, NextFunction } from "express";


const seedNotification = async (req: Request, res: Response, next: NextFunction) => {
    
}